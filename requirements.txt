# Core runtime
playwright>=1.46
httpx>=0.28
python-dotenv>=1.0
pyyaml>=6.0
unidecode>=1.3.8

# Persistencia
# SQLite vem com a stdlib; nenhuma dep extra necessaria pra MVP.

# LLM clients (escolha um pra comecar)
anthropic>=0.40
# openai>=1.50  # opcional

# Image / carrossel (Phase 2)
pillow>=10.4
# fal-client>=0.5  # se for usar fal.ai pra capas

# Dev / testes
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.6
black>=24.8
